import { Root } from "../root";

export class CountriesController {
  root: Root;
  constructor(_root: Root) {
    this.root = _root;
  }

  async createCountry() {}
  async updateCountry() {}
  async deleteCountry() {}
  async getCountry() {}
}
